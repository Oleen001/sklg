
  # Add transitions and animations

  This is a code bundle for Add transitions and animations. The original project is available at https://www.figma.com/design/G4j2TMMOZHHiJYBidncRcQ/Add-transitions-and-animations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  